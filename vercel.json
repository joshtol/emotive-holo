{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "sed -i 's|\"file:../emotive-engine\"|\"^3.2.2\"|' package.json && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
